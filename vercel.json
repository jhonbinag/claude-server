{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "cd frontend && npm install && npm run build",
  "functions": {
    "api/index.js": {
      "includeFiles": "src/**"
    }
  },
  "rewrites": [
    { "source": "/ui/assets/(.*)", "destination": "/ui/assets/$1" },
    { "source": "/ui/(.*)",        "destination": "/ui/index.html" },
    { "source": "/(.*)",           "destination": "/api" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
